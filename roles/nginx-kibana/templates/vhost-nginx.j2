server {
	listen 80;

	server_name {{ domain }};
	access_log /home/{{ username }}/logs/access.log;
        error_log /home/{{ username }}/logs/error.log warn;

        location / {
        	root  /home/{{ username }}/public_html/kibana-{{ kibana_version}};
    		index index.html index.htm;
		auth_basic "Restricted";
		auth_basic_user_file {{ nginx_confd }}/{{ domain }}.htpasswd;
  	}
}

